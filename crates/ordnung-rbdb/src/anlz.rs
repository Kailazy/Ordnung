//! ANLZ `.DAT`/`.EXT` writer (Phase 5).
//!
//! Serializes beatgrid (`PQTZ`), cues (`PCOB`/`PCO2`), path (`PPTH`), and
//! waveforms (`PWAV`/`PWV*`) into the tagged-section format CDJs require.
//! See the `rekordbox-format` skill.

// Intentionally empty until Phase 5.

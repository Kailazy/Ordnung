//! DeviceSQL `export.pdb` writer (Phase 5).
//!
//! Builds the track/artist/album/genre/key/playlist tables that CDJs read.
//! See the `rekordbox-format` skill for table layout, DeviceSQLString handling,
//! BPM = round(bpm*100), and the round-trip validation workflow.

// Intentionally empty until Phase 5.
